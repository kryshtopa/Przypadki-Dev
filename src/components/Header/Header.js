import React from 'react';

const header = () => (
	<header className="mdl-layout__header">
		<div id="progressBar" className="mdl-progress mdl-js-progress mdl-progress--red"></div>
	</header>
)

export default header;