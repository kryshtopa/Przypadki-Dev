import React from 'react';

import CasesQuiz from '../../containers/CasesQuiz/CasesQuiz';

const main = (props) => (
	<main className="mdl-layout__content wrapper">
		<CasesQuiz/>
  </main>
)

export default main;